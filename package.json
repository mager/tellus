{
  "name": "tellus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "publish": "gcloud builds submit --tag gcr.io/geotory/tellus",
    "deploy": "gcloud run deploy --image gcr.io/geotory/tellus --platform managed --region us-central1 --port 3005 --allow-unauthenticated tellus",
    "ship": "git push origin main && npm run publish && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": "19.x"
  }
}
